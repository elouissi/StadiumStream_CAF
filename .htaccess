#obligatoire going to public folder (show just folder of public not all the app)

<IFModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IFModule>